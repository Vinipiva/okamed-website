---
const totalNumbered = 33;
const clientImages: { src: string; alt: string }[] = Array.from(
  { length: totalNumbered },
  (_, i) => ({
    src: `/clients/client_okamed-${i + 1}.png`,
    alt: ``,
  })
);
clientImages.push({ src: `/clients/client_okamed.png`, alt: `` });

const locale = Astro.currentLocale ?? (Astro.url.pathname.startsWith('/es/') ? 'es' : 'pt');
const isEs = locale === 'es';
const label = isEs ? 'Socios y clientes' : 'Parceiros e clientes';
---

<div class="clients-wrapper">
  <div class="clients-label">{label}</div>
  <div class="clients-strip">
    <div class="carousel-track">
      <div class="carousel-set" id="carousel-primary">
        {clientImages.map((img, idx) => (
          <div class="client-card">
            <img
              src={img.src}
              width="192"
              height="88"
              loading="lazy"
              decoding="async"
              alt={isEs
                ? `Logo de cliente o socio de Okamed número ${idx + 1}`
                : `Logo de cliente ou parceiro da Okamed número ${idx + 1}`}
            />
          </div>
        ))}
      </div>
      <div class="carousel-set" id="carousel-clone" aria-hidden="true"></div>
    </div>
  </div>
</div>

<script>
  const primary = document.getElementById("carousel-primary");
  const clone = document.getElementById("carousel-clone");

  if (primary && clone) {
    const cards = Array.from(primary.querySelectorAll<HTMLElement>(".client-card"));

    for (let i = cards.length - 1; i > 0; i--) {
      const j = Math.floor(Math.random() * (i + 1));
      [cards[i], cards[j]] = [cards[j], cards[i]];
    }

    primary.replaceChildren(...cards);

    cards.forEach((card) => {
      const cloned = card.cloneNode(true) as HTMLElement;
      const img = cloned.querySelector("img");
      if (img) img.alt = "";
      clone.appendChild(cloned);
    });
  }
</script>

<style>
.clients-wrapper {
  width: 100%;
  background: rgba(244,246,248,0.80);
  backdrop-filter: blur(16px);
  border-top: 1px solid rgba(255,255,255,0.80);
  padding: 28px 0 32px;
}

.clients-label {
  text-align: center;
  font-size: 0.65rem;
  font-weight: 600;
  letter-spacing: 0.3em;
  text-transform: uppercase;
  color: rgba(11,46,78,0.25);
  margin-bottom: 18px;
}

.clients-strip {
  overflow: hidden;
  mask-image: linear-gradient(to right, transparent 0%, black 8%, black 92%, transparent 100%);
  -webkit-mask-image: linear-gradient(to right, transparent 0%, black 8%, black 92%, transparent 100%);
}

.carousel-track {
  display: flex;
  gap: 40px;
  width: max-content;
  animation: scroll 120s linear infinite;
  will-change: transform;
}

.carousel-track:hover { animation-play-state: paused; }

.carousel-set {
  display: flex;
  align-items: center;
  gap: 40px;
}

.client-card {
  flex: 0 0 auto;
  width: 240px;
  height: 120px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 18px;
  background: rgba(255,255,255,0.90);
  border: 1px solid rgba(255,255,255,0.95);
  box-shadow: 0 2px 12px rgba(11,46,78,0.07);
  padding: 16px 24px;
  transition: background 0.25s, box-shadow 0.25s, transform 0.25s;
}

.client-card:hover {
  background: rgba(255,255,255,1);
  box-shadow: 0 8px 24px rgba(11,46,78,0.12);
  transform: translateY(-2px);
}

.client-card img {
  width: 100%;
  height: 100%;
  object-fit: contain;
  opacity: 0.85;
  transition: opacity 0.25s;
}

.client-card:hover img { opacity: 1; }

@keyframes scroll {
  from { transform: translateX(0); }
  to   { transform: translateX(-50%); }
}
</style>
